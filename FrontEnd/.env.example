# Baked in at build time (CRA). Set the same key in Vercel Environment Variables.
# No trailing slash.
REACT_APP_API_URL=https://YOUR-RENDER-SERVICE.onrender.com

# External API keys are configured on the backend only:
# GOOGLE_MAPS_API_KEY, UNSPLASH_ACCESS_KEY
